apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: slurm-db
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ezrizhu/puppy
    targetRevision: main
    path: manifests/slurm
  destination:
    server: https://kubernetes.default.svc
    namespace: slurm
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - SkipDryRunOnMissingResource=true
    retry:
      limit: 10
      backoff:
        duration: 15s
        factor: 2
